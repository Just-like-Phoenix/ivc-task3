import theme from "styled-theming";

export const blockColor = theme("mode", {
  light: "#ffffff",
  dark: "#1a1a1b",
});

export const backgroundColor = theme("mode", {
  light: "#dae0e6",
  dark: "#030303",
});

export const selectedColor = theme("mode", {
  light: "#0f60b6",
  dark: "#2f78c5",
});

export const textColor = theme("mode", {
  light: "#1c1c1c",
  dark: "#d7dadc",
});

export const buttonColor = theme("mode", {
  light: "#dae0e6",
  dark: "#3e3e41",
});
